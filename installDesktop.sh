#prepare install with update first:
#sudo apt update
#sudo apt upgrade
#sudo apt dist-upgrade
#sudo reboot

sudo apt install xserver-xorg
sudo apt install raspberrypi-ui-mods
sudo apt install lightdm
